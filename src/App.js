import React from "react";
import "./styles.css";
import AppContainer from "./components/AppContainer";
export default function App() {
  return (
    <div className="App">
      {" "}
      <AppContainer />{" "}
    </div>
  );
}
//basic layout - ui library can be use
//restarent based availability
// availability toggle on each item
// all available = change all item to available and vice versa
// search = keyword search based on restaurent
